axes.titlesize : 10
axes.labelsize : 11
lines.linewidth : 1.2
xtick.labelsize : 9
ytick.labelsize : 9
legend.fontsize : 9
figure.figsize : 4.13, 2.55 # Height and width in inches. Width should be adjusted to journal column width, height computed from golden ratio.
figure.dpi : 600 # Pixels per inch. 300 and 600 are typical values.
font.family : serif
text.usetex : False # Rendering with text is VERY slow. Only do it at the end.